import React from 'react'

const TextError = (props) => {
    return (
        <div className='errortxt'>

            {props.children}
            
        </div>
    )
}

export default TextError


